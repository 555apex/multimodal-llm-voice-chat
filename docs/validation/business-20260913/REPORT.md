# DGX 后端业务同步验收记录（2026-09-13）

## 发布内容

- 上游基准：`version/roadagent-v1` / `4dab57257eaf09e435245f46dba650fa9a258397`。
- DGX 起点：`roadagent-v2` / `4cd7197acfc8866b66eb415b5a8daed1feb2286f`。
- 本次运行代码提交：`3b746df`；逐项合入上游 `8e78975`、`16f7204`、`e046260`、`4dab572`，保留原发布历史。
- 应急预案资源解耦、v3 空资源基线兼容、重试快照与事件类型匹配恢复；保留版本领取条件、字段简写和库存事务保护。
- 应急格式和业务纠错合计一次，最多两次模型调用。单次上限180秒，总预算360秒；超时、连接失败及截断不触发内容纠错。
- 通行能力阈值统一为60%/80%，固定问答和模型提示共用领域口径。前端仅修改一处空结果提示，数字人及语音前端文件未变。
- 完整测试发现旧语音适配器的异常分支绕过了已有的415/429/504等映射；本次修正异常转交，恢复原测试要求的行为。
- 数据库预检支持16类完整活动预案及空基线，不再硬性要求活动版本标记为 `compact-20260910`。

## 验证结果

- DGX Java 17 容器中执行 `./mvnw -B -o verify`：BUILD SUCCESS。222项测试，205项通过，17项依赖显式数据库/在线模型测试开关的测试按原条件跳过，失败与错误均为0。
- 前端 Vitest：26个测试文件、136项全部通过；生产构建通过。
- 生产逻辑备份恢复到独立MySQL容器，隔离schema为 `road_agent_qa_20260913`；16类活动预案全部为v3、空资源基线。
- 隔离后端复用生产本地Qwen，首次生成5.892秒（含一次纠错），市级退回重生成2.966秒。退回要求的 `MOBILE_LIGHTING` 已进入资源需求，方案版本递增且库存流水守恒。这是各一次功能样本，不是P95或长期性能结论。
- 隔离库实际通行能力响应全部符合60%/80%分级；单测覆盖边界、旧阈值问答以及混合格式/业务错误最多两次调用。
- 候选主界面通过SSH隧道完成浏览器抽查：数字人可见，录音可用。远程演示页导航超时；使用同一份构建产物在本地完成演示页和素材渲染检查，无页面脚本异常。
- 候选打包曾因静态目录权限导致白屏，已修正目录755、文件644及镜像文件属主，并验证静态资源的内容类型。
- 发布后主界面、数字人演示页均返回200，其脚本和样式资源可读；两个后端语音能力和通告接口通过，启动后错误日志计数为0。
- 公网本地入口与HTTPS入口均返回预期登录认证401；登录文件摘要未变。
- 发布后核对534个源码文件，与候选清单一致。未进行15分钟观察、长期录音测试或负载测试。

## 运行基准与数据保护

- 后端镜像：`road-agent-dgx-backend:business-20260913`。
- 前端镜像：`road-agent-dgx-frontend:business-20260913`。
- 语音镜像保持 `road-agent-dgx-speech:speech-20260910`，本地Qwen3-TTS、Serena、faster-whisper small CPU/int8保持原配置。
- LLM保持本地 `qwen3.6-35b-a3b-nvfp4`，`enable_thinking=false`。模型与语音容器ID、启动时间均未变化。
- 内外网数据库连接和凭据未变；自动分类仍仅公网后端启用。本次没有执行迁移、预案发布、数据清理或生产工单生成操作。

## 备份与证据

- DGX备份目录：`/home/whtc/workspace/backups/road-agent-business/20260913T025307Z`。
- `source.tar.gz`：更新前源码；`containers.json`：原镜像与容器配置；`runtime.env`、`public.env`：受保护的原配置。
- `database.sql`：隔离恢复使用的备份；`maintenance-database.sql`：维护期停写后的最终备份；`maintenance-runtime.env`：切换前最终运行配置。
- DGX发布日志及验收证据：`/home/whtc/workspace/roadagent-releases/business-20260913/`。
- Windows浏览器截图与验收记录：工作区 `audit/business-20260913/`。
- 凭据、数据库备份及包含运行环境的容器详情不提交Git。

旧业务镜像不能正确读取共享库已发布的v3空基线预案，因此不作为可直接恢复应急生成的回滚版本。如本版出现问题，应保持受影响入口维护并修复候选；不得为应用回滚执行数据库端点回退、恢复旧预案或清理业务数据。旧镜像和源码保留用于定位与制作兼容修复版本。
