# 基于两模态LLM的语音聊天系统 - 本地部署版 (FunASR + ChatTTS)

## 1. 项目概述

### 1.1 项目名称
基于两模态（文本+语音）LLM 的语音聊天系统 - 本地部署版

### 1.2 项目目标
构建一个支持文本和语音两种输入模态的智能聊天系统，ASR 和 TTS 采用本地部署的开源模型，实现低延迟、高质量的语音交互体验。

### 1.3 技术选型

| 组件 | 技术方案 | 部署方式 |
|------|----------|----------|
| 前端框架 | Vue 3 | 本地 |
| 后端框架 | Python Flask | 本地 |
| 前后端通信 | WebSocket | 本地 |
| 语音识别 (ASR) | FunASR (Paraformer-zh) | 🖥️ 本地部署 |
| 语音合成 (TTS) | ChatTTS | 🖥️ 本地部署 |
| 大语言模型 (LLM) | mimo-v2.5-pro | ☁️ API 调用 |
| 卡通人物 | CSS 动画 | 本地 |
| 部署方式 | 本地运行 | - |

### 1.4 硬件要求

| 组件 | 最低配置 | 推荐配置 |
|------|----------|----------|
| GPU | NVIDIA 8GB 显存 | NVIDIA 16GB+ 显存 |
| 内存 | 16GB | 32GB |
| 硬盘 | 10GB 可用空间 | 20GB+ |
| CPU | 4 核 | 8 核+ |

---

## 2. 功能需求

### 2.1 输入功能

| 功能 | 描述 |
|------|------|
| 文本输入 | 用户通过键盘在输入框中输入文本，点击发送或回车提交 |
| 语音输入 | 用户点击语音按钮开始录音，再次点击停止录音，录音结束后自动识别并发送 |

### 2.2 处理功能

| 功能 | 描述 |
|------|------|
| 语音识别 | 使用 FunASR (Paraformer-zh) 本地识别语音为文本 |
| LLM 推理 | 调用 mimo-v2.5-pro API 生成智能回复 |
| 语音合成 | 使用 ChatTTS 本地合成语音 |

### 2.3 输出功能

| 功能 | 描述 |
|------|------|
| 文本显示 | 在对话框中显示用户输入和 LLM 回复（流式输出） |
| 语音朗读 | LLM 回复完成后自动朗读（默认开启） |
| 口型动画 | 朗读时卡通人物显示说话口型 |
| 音量控制 | 用户可调节朗读音量 |
| 朗读开关 | 用户可开启/关闭自动朗读 |

---

## 3. 系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         前端 (Vue 3)                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│  │ 输入框      │  │ 语音按钮    │  │ 卡通人物 + 口型动画      │ │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘ │
└───────────────────────────────┬─────────────────────────────────┘
                                │ WebSocket
┌───────────────────────────────┴─────────────────────────────────┐
│                       后端 (Python Flask)                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  WebSocket Handler                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│         │                    │                    │             │
│         ▼                    ▼                    ▼             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │  ASR 模块   │    │  LLM 模块   │    │  TTS 模块   │         │
│  │  (FunASR)   │    │  (mimo API) │    │  (ChatTTS)  │         │
│  │  🖥️ 本地    │    │  ☁️ API     │    │  🖥️ 本地    │         │
│  └─────────────┘    └─────────────┘    └─────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 模块说明

| 模块 | 职责 | 部署方式 |
|------|------|----------|
| **WebSocket Handler** | 处理前后端实时通信 | 本地 |
| **ASR 模块** | 使用 FunASR 进行语音识别 | 本地 GPU |
| **LLM 模块** | 调用 mimo-v2.5-pro API | 云端 |
| **TTS 模块** | 使用 ChatTTS 进行语音合成 | 本地 GPU |

---

## 4. 工作流程

### 4.1 文本输入流程

```
用户输入文本 → WebSocket → LLM API (流式) → 流式显示 → ChatTTS 合成 → 播放音频
```

### 4.2 语音输入流程

```
用户录音 → WebSocket → FunASR 识别 → 显示识别文本 → LLM API (流式) → 流式显示 → ChatTTS 合成 → 播放音频
```

---

## 5. 文件结构

```
version_funasr_chattts/
├── 系统设计规划书.md              # 本文档
├── README.md                      # 项目说明
│
├── frontend/                      # 前端 Vue 项目
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── components/
│       │   ├── ChatWindow.vue
│       │   ├── MessageBubble.vue
│       │   ├── InputArea.vue
│       │   ├── CharacterAvatar.vue
│       │   ├── VoiceButton.vue
│       │   └── AudioControls.vue
│       └── stores/
│           └── chatStore.js
│
├── backend/                       # 后端 Flask 项目
│   ├── requirements.txt
│   ├── config.py
│   ├── .env.example
│   ├── app.py
│   ├── api/
│   │   └── websocket.py
│   ├── services/
│   │   ├── asr_service.py         # FunASR 服务
│   │   ├── llm_service.py         # mimo API 服务
│   │   └── tts_service.py         # ChatTTS 服务
│   └── models/                    # 模型文件目录
│       └── .gitkeep
│
├── audio_wav/
│   ├── user_query/
│   └── llm_answer/
│
└── assets/
    └── avatar/
```

---

## 6. 依赖说明

### 6.1 Python 依赖 (后端)

```
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.10.0
requests>=2.31.0
python-dotenv>=1.0.0

# ASR - FunASR
funasr>=1.0.0
modelscope>=1.10.0
torch>=2.0.0
torchaudio>=2.0.0

# TTS - ChatTTS
ChatTTS>=0.2.0
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0
```

### 6.2 模型下载

**ASR 模型 (FunASR Paraformer-zh)：**
- 模型名称：`iic/speech_paraformer-large-vad-punc_asr_nat-zh-cn-16k-common-vocab8404-pytorch`
- 首次运行自动下载，约 1GB

**TTS 模型 (ChatTTS)：**
- 模型名称：`2Noise/ChatTTS`
- 首次运行自动下载，约 2-3GB

---

## 7. API 接口设计

### 7.1 前后端 WebSocket 消息格式

**客户端 → 服务端：**

```json
// 文本消息
{
  "type": "text",
  "content": "用户输入的文本",
  "auto_read": true
}

// 语音消息
{
  "type": "audio",
  "content": "base64编码的音频数据",
  "format": "wav",
  "auto_read": true
}
```

**服务端 → 客户端：**

```json
// LLM 流式文本回复
{
  "type": "text_chunk",
  "content": "当前生成的文字片段"
}

// LLM 回复完成
{
  "type": "text_complete",
  "content": "完整的回复文本"
}

// TTS 音频数据
{
  "type": "audio",
  "content": "base64编码的音频数据",
  "format": "wav"
}

// ASR 识别结果
{
  "type": "asr_result",
  "content": "识别出的文本"
}
```

---

## 8. 部署步骤

### 8.1 环境准备

```bash
# 创建虚拟环境
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac

# 安装 PyTorch (CUDA 版本)
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

# 安装其他依赖
pip install -r requirements.txt
```

### 8.2 配置

```bash
cp .env.example .env
# 编辑 .env 填入 MIMO_API_KEY
```

### 8.3 启动

```bash
# 后端
cd backend
python app.py

# 前端 (新终端)
cd frontend
npm install
npm run dev
```

---

## 9. 性能预期

| 指标 | 预期值 |
|------|--------|
| ASR 识别延迟 | < 500ms (5秒音频) |
| TTS 合成延迟 | < 1s (50字) |
| LLM 首字延迟 | < 1s (API) |
| 总响应延迟 | < 2s |
| 显存占用 | ~4GB |

---

## 10. 待确认事项

| 序号 | 事项 | 状态 |
|------|------|------|
| 1 | MIMO_API_KEY | 待用户提供 |
| 2 | 卡通人物素材 | 待用户补充 |
