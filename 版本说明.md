# 基于两模态LLM的语音聊天系统 - 版本说明

## 项目概述

本项目包含三个独立版本，用于对比测试不同的 ASR 和 TTS 技术方案。

---

## 版本列表

| 版本 | 文件夹 | ASR | TTS | 特点 |
|------|--------|-----|-----|------|
| **版本1: API 调用版** | `version_api/` | mimo-v2.5-asr (API) | mimo-v2.5-tts (API) | 无需 GPU，依赖网络 |
| **版本2: FunASR + ChatTTS** | `version_funasr_chattts/` | FunASR (本地) | ChatTTS (本地) | 中文最佳，对话自然 |
| **版本3: Whisper + CosyVoice** | `version_whisper_cosyvoice/` | Whisper (本地) | CosyVoice (本地) | 多语言，高音质 |

---

## 硬件要求对比

| 版本 | GPU 要求 | 显存占用 | 推荐配置 |
|------|----------|----------|----------|
| 版本1 (API) | ❌ 不需要 | 0GB | 任意电脑 |
| 版本2 (FunASR+ChatTTS) | ✅ 需要 | ~4GB | NVIDIA 8GB+ |
| 版本3 (Whisper+CosyVoice) | ✅ 需要 | ~8GB | NVIDIA 16GB+ |

---

## 功能对比

| 功能 | 版本1 | 版本2 | 版本3 |
|------|-------|-------|-------|
| 文本输入 | ✅ | ✅ | ✅ |
| 语音输入 | ✅ | ✅ | ✅ |
| 流式输出 | ✅ | ✅ | ✅ |
| 语音朗读 | ✅ | ✅ | ✅ |
| 卡通人物 | ✅ | ✅ | ✅ |
| 中文识别 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 多语言识别 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| TTS 自然度 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| TTS 音质 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 响应速度 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 模型下载路径配置

版本2和版本3的模型默认下载到 D 盘指定文件夹：

| 版本 | 模型路径 | 包含模型 |
|------|----------|----------|
| 版本2 | `D:\funasr_chattts\` | FunASR、ChatTTS |
| 版本3 | `D:\whisper_cosyvoice\` | Whisper、CosyVoice |

如需修改路径，编辑对应版本的 `backend/.env` 文件：
```
MODEL_CACHE_DIR=你的自定义路径
```

---

## 目录结构

```
基于两模态llm的语音聊天系统/
├── 版本说明.md                          # 本文档
│
├── version_api/                         # 版本1: API 调用版
│   ├── 系统设计规划书.md
│   ├── README.md
│   ├── frontend/
│   └── backend/
│
├── version_funasr_chattts/              # 版本2: FunASR + ChatTTS
│   ├── 系统设计规划书.md
│   ├── README.md
│   ├── frontend/
│   └── backend/
│
└── version_whisper_cosyvoice/           # 版本3: Whisper + CosyVoice
    ├── 系统设计规划书.md
    ├── README.md
    ├── frontend/
    └── backend/
```

---

## 启动方法

每个版本的启动方法相同：

### 1. 安装依赖

```bash
# 进入对应版本目录
cd version_xxx

# 后端依赖
cd backend
pip install -r requirements.txt

# 前端依赖
cd ../frontend
npm install
```

### 2. 配置 API Key

```bash
cp backend/.env.example backend/.env
# 编辑 .env 文件，填入 MIMO_API_KEY
```

### 3. 启动服务

```bash
# 启动后端 (终端1)
cd backend
python app.py

# 启动前端 (终端2)
cd frontend
npm run dev
```

### 4. 访问应用

浏览器打开：`http://localhost:3000`

---

## 推荐使用场景

### 版本1: API 调用版
- **适合**：没有 GPU 的用户
- **适合**：快速体验和测试
- **适合**：不想安装复杂的本地模型

### 版本2: FunASR + ChatTTS
- **适合**：中文对话场景
- **适合**：追求自然对话体验
- **适合**：显存 8GB+ 的用户

### 版本3: Whisper + CosyVoice
- **适合**：多语言场景
- **适合**：追求高音质
- **适合**：显存 16GB+ 的用户

---

## 技术栈

| 组件 | 版本1 | 版本2 | 版本3 |
|------|-------|-------|-------|
| 前端 | Vue 3 | Vue 3 | Vue 3 |
| 后端 | Flask | Flask | Flask |
| 通信 | WebSocket | WebSocket | WebSocket |
| ASR | mimo API | FunASR | Whisper |
| TTS | mimo API | ChatTTS | CosyVoice |
| LLM | mimo API | mimo API | mimo API |

---

## 注意事项

1. **三个版本共享前端端口**（3000）和后端端口**（5000），同一时间只能运行一个版本

2. **版本2和版本3首次启动**会自动下载模型，请确保网络畅通

3. **版本3的 CosyVoice** 安装可能需要额外步骤，请参考官方文档

4. **所有版本都需要** mimo 开放平台 API Key（用于 LLM）

---

## 测试建议

1. 先测试 **版本1**，确保基本功能正常
2. 再测试 **版本2**，体验本地部署的效果
3. 最后测试 **版本3**，对比不同方案的差异

---

## 问题反馈

如遇到问题，请查看对应版本的 README.md 或系统设计规划书。

---

## 更新日志

### 2026年6月18日

**版本2更新**:
1. LLM API 从 mimo 切换为 DeepSeek
2. 修复对话卡在"思考中"的问题
3. UI人物形象改为交警制服卡通人物

**配置变更**:
- 新增 `LLM_PROVIDER`、`LLM_API_KEY`、`LLM_API_BASE_URL`、`LLM_MODEL` 配置项
- DeepSeek API: https://api.deepseek.com
- 默认模型: deepseek-chat
